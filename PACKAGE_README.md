# Complete replacement package

This archive contains the full FantasyNextMove project from the current `main` branch with Beta Admin merged into the correct locations.

Do not merge its top-level `app`, `components`, or `lib` folders into a damaged partial folder. Replace the damaged local project folder with this complete project folder, then commit and push.

Required Vercel Production variable:

```text
FNM_ADMIN_EMAILS=csgentry@outlook.com
```
